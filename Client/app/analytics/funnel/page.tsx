import {Bar,Insight,PageHead,Panel} from '@/components/analytics/Ui';
const steps=[['Product viewed',10000,100],['Added to cart',4200,42],['Checkout started',2400,24],['Payment started',2050,20.5],['Purchased',1750,17.5]] as const;
export default function Page(){return <><PageHead title="Conversion Funnel" description="A staged view of the purchase journey used to identify where intent weakens and where product teams should investigate friction."/><Panel title="Discovery → purchase"><div>{steps.map(([n,c,p])=><Bar key={n} label={n} value={p} note={`${c.toLocaleString()} users · ${p}%`}/>)}</div></Panel><div className="mt-6"><Insight title="Analyst readout">The modeled biggest relative loss is from product view to add-to-cart. Segmenting by device, source, category and new vs. returning shoppers would be the next analysis step.</Insight></div></>}
