import {Insight,PageHead,Panel} from '@/components/analytics/Ui';
const steps=['Home','Search / category','Product detail','Wishlist / cart','Checkout','Order confirmation'];
export default function Page(){return <><PageHead title="Customer Journey Analysis" description="A simplified journey model that combines behavioral telemetry with UX questions to surface friction across discovery and purchase."/><Panel title="Most common modeled journey"><div className="flex flex-col gap-3 md:flex-row md:items-center">{steps.map((s,i)=><div key={s} className="flex items-center gap-3"><div className="rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-semibold">{s}</div>{i<steps.length-1&&<span className="hidden text-slate-300 md:block">→</span>}</div>)}</div></Panel><div className="mt-6 grid gap-4 md:grid-cols-2"><Insight title="Journey hypothesis">Returning shoppers who resume from recently viewed products should need fewer discovery interactions before carting.</Insight><Insight title="Measurement plan">Track page_view, search_performed, product_viewed, wishlist_added, product_added_to_cart, checkout_started and order_completed by session.</Insight></div></>}
