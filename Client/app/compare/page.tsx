import BrandLogo from '@/components/BrandLogo';
import Link from 'next/link';
const products=[
 {name:'Everyday Sneakers',price:'$72',rating:'4.6',delivery:'2–3 days',fit:'True to size',returns:'30 days'},
 {name:'City Runner',price:'$89',rating:'4.8',delivery:'3–4 days',fit:'Runs small',returns:'30 days'},
 {name:'Cloud Walk',price:'$64',rating:'4.4',delivery:'2 days',fit:'True to size',returns:'14 days'},
];
export default function Compare(){return <main className="min-h-screen bg-slate-50"><header className="border-b bg-white"><div className="mx-auto flex max-w-[1200px] items-center justify-between px-5 py-5"><BrandLogo/><Link href="/" className="text-sm font-semibold">← Continue shopping</Link></div></header><section className="mx-auto max-w-[1200px] px-5 py-10"><p className="text-xs font-bold uppercase tracking-[.18em] text-emerald-700">Decision support</p><h1 className="mt-2 text-4xl font-bold">Compare products with less guesswork</h1><p className="mt-3 max-w-2xl text-slate-600">This portfolio module demonstrates a comparison experience designed to reduce choice overload. Connect it to live selected products as a next enhancement.</p><div className="mt-8 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[760px] text-left"><thead><tr className="bg-slate-950 text-white"><th className="p-4">Attribute</th>{products.map(p=><th className="p-4" key={p.name}>{p.name}</th>)}</tr></thead><tbody>{[['Price','price'],['Rating','rating'],['Delivery','delivery'],['Fit guidance','fit'],['Returns','returns']].map(([label,key])=><tr key={key} className="border-t"><td className="p-4 font-semibold text-slate-500">{label}</td>{products.map(p=><td className="p-4" key={p.name}>{p[key as keyof typeof p]}</td>)}</tr>)}</tbody></table></div><div className="mt-6 rounded-2xl bg-emerald-50 p-5 text-sm leading-6 text-emerald-950"><strong>Product analytics angle:</strong> track compare_opened, compare_product_added and compare_to_cart events to learn whether comparison reduces uncertainty and improves downstream conversion.</div></section></main>}
